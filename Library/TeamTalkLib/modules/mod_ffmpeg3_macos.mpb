project : mod_ffmpeg3 {

specific(make,gnuace){
  linkflags += "-framework Cocoa -framework AVFoundation"
  linkflags += "-framework CoreMedia -framework OpenGL"
  linkflags += "-framework CoreFoundation -framework VideoDecodeAcceleration"
  linkflags += "-framework QuartzCore"

  lit_libs += z
}

Header_Files {

}

Source_Files {

}
  
}

